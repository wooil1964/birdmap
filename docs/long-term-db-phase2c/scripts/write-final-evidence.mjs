import assert from 'node:assert/strict';
import {readFileSync,writeFileSync,readdirSync} from 'node:fs';
import {join} from 'node:path';
import {execFileSync} from 'node:child_process';
import {LOCAL,ROOT,ACCOUNT,STAGING_DB,PRODUCTION_DB,WORKERS} from './staging-control.mjs';
const dir=join(ROOT,'docs/long-term-db-phase2c'),read=n=>JSON.parse(readFileSync(join(LOCAL,n),'utf8'));
const schema=read('schema-verification.json'),prep=read('backfill-preparation.json'),remote=read('remote-main-current.json'),preservation=read('preservation-current.json'),start=read('cloudflare-preflight.json'),end=read('cloudflare-end.json'),limits=read('fixture-limits.json'),final=read('final-staging-state.json');
const suites=[['initial','initial-remote-tests.json'],['Access','access-maintenance-smoke.json'],['backfill','backfill-remote-results.json'],['core','core-remote-tests.json'],['rollout_v3','rollout-v3-remote-tests.json'],['purge','purge-remote-tests.json'],['Turnstile','turnstile-remote-tests.json'],['cutover','cutover-rehearsal.json'],['recovery','recovery-remote-tests.json']].map(([name,file])=>{const r=read(file);assert(!r.status||r.status==='PASS',name);const tests=r.tests||r.steps;assert(tests.every(t=>t.result==='PASS'),name);return {name,file,passed:tests.length,tests,started:r.at,finished:r.finished_at,scope:r.scope};});
assert.equal(schema.classification,'A_MATCH');assert.equal(preservation.unexpected.length,0);assert(preservation.head_unchanged);assert.equal(remote.unexpected.length,0);
const productionUnchanged=JSON.stringify(start.production.workers)===JSON.stringify(end.production.workers);assert(productionUnchanged);
assert(final.workers.every(w=>w.stagingBinding&&w.stagingPeer&&w.mode==='READ_ONLY_MAINTENANCE'&&w.build==='phase2c-dual-v3'));assert.equal(final.state.counts.transaction_assertions,0);assert.deepEqual(final.state.fk,[]);
const local=JSON.parse(readFileSync(join(ROOT,'docs/long-term-db-phase2b/.local/phase2b-local-results.json'),'utf8')),gate=read('gate-local-regression.json');assert.equal(local.status,'PASS');assert.equal(local.tests.length,32);assert.equal(gate.tests.length,13);
for(const [file,count] of [['legacy106-final.log',106],['protocol-final.log',5]]){const text=readFileSync(join(LOCAL,file),'utf8');assert(text.includes('pass '+count)&&text.includes('fail 0'));}
assert.equal((readFileSync(join(LOCAL,'phase2a-final.log'),'utf8').match(/^PASS /gm)||[]).length,17);
const counters={production_d1_writes:0,production_migrations:0,production_worker_deploys:0,production_route_changes:0,production_secret_changes:0,production_service_binding_changes:0,production_access_changes:0,production_exports:0,production_restores:0,commit:0,push:0};
const risks=[
 'Production 적용은 승인되지 않았으며 NO-GO. 실제 외부 writer freeze·구 Worker drain·배포 책임자/중단 기준을 별도로 확정해야 한다. staging wrapper freeze는 production 방화벽 설정 검증이 아니다.',
 'Turnstile 공식 pass/fail/already-spent Siteverify와 동시 멱등성은 통과했다. 실제 브라우저 발급 single-use token 및 CAPTCHA 실패 직후 concurrent commit 재조회 분기 실행은 원격에서 특정하지 않았다(해당 분기는 로컬 시험 통과).',
 '새 staging DB의 생성 직후 요청 시점 bookmark와 실제 복구는 확인했다. 계정 구독에 따른 최대 7일/30일 보존 한도는 계정 수준에서 확정하지 않았다.',
 'Time Travel의 purge 이전 이력에는 제거 자료가 남을 수 있다. 이번 복구는 purge 이후 bookmark로만 수행했다. 운영 복구 시 금지 자료 재유입 방지 절차가 필요하다.',
 'legacy purge는 PURGE_LEGACY_DECISION_REQUIRED를 유지한다. 운영자가 보존 정책과 금지자료 삭제 예외를 결정해야 한다. 현재 purge는 권한 있는 내부 함수이며 운영용 인증 진입점을 새로 공개하지 않았다.',
 '64/190 statement batch의 성공은 대규모 부하 증명이 아니다. 미래 chunk backfill은 현재 partial-state 거부·manifest 원칙을 유지하는 별도 설계와 한도 검증이 필요하다.',
 '일반 DELETE 금지는 application 경계이고 직접 D1 관리자 권한에는 적용되지 않는다. Worker rollback은 DB를 되돌리지 않으며, 이번에는 안전한 maintenance 버전만 복귀했다.',
 '이번 14단계 cutover 재연은 기설치 staging에서 migration/seed no-op 및 backfill cohort 검증으로 수행했다. 최초 빈 DB migration/backfill 성공 증거와 구분한다. production 데이터·트래픽·배포 drain을 시험한 것은 아니다.'
];
const result={status:'PHASE_2C_COMPLETE_WITH_DOCUMENTED_LIMITATIONS',generated_at:new Date().toISOString(),production_decision:'NO_GO_UNTIL_SEPARATE_REVIEW_AND_AUTHORIZATION',phase2d_started:false,resources:{account:ACCOUNT,staging_database:STAGING_DB,production_database:PRODUCTION_DB,workers:WORKERS,isolated:true},schema:{classification:schema.classification,hash:schema.remote_hash,tables:9,columns:114,reports_columns:22},remote_checks:1+suites.reduce((n,s)=>n+s.passed,0),suites,local:{phase2b:32,legacy:106,phase2a:17,protocol:5,focused_gate:13,all_passed:true,legacy_count_note:'105 assertions + helpers.mjs file execution = historical106'},remote_main:remote,preservation,final_staging:final,production:{checked_at:end.checked_at,workers_equal_start:productionUnchanged,counts:end.production.counts.result?.[0]?.results?.[0]},limits,risks,counters};
writeFileSync(join(dir,'test-results.json'),JSON.stringify(result,null,2)+'\n');
const rows=[
 ['A','시작 상태와 remote main',`main / HEAD ${preservation.head}. 최신 remote main ${remote.remote_main}; 날씨·조석 JSON 5개만 차이. fetch/pull/merge 없음.`],
 ['B','staging 격리',`D1 ${STAGING_DB}; ${WORKERS.join(' / ')}. 실제 DB/peer binding 고정 검증. production D1 ${PRODUCTION_DB}와 다름. staging Access만 생성·사용.`],
 ['C','remote migration','공식 Wrangler 0000_legacy + Phase2A 0001_core만 staging에 적용. 재적용과 cutover 재연은 no-op. 별도 schema ledger 추가 없음.'],
 ['D','schemaReady',`A_MATCH; remote/local/hash 동일 ${schema.remote_hash}. 9테이블·114컬럼 및 reports22컬럼, DDL/PRAGMA/인덱스/FK/trigger 비교 일치.`],
 ['E','batch rollback','실제 D1 quick 중간·후반 실패, admin 실패, purge 중간·tombstone 이후 실패에서 전체 rollback. 독립 run으로 분할하지 않음.'],
 ['F','changes / CAS','실제 UPDATE → SELECT changes() → assertion: 정상1, stale실패, 동시 revision 경쟁1commit. 다른 DML 삽입 없음. 최종 assertions0.'],
 ['G','idempotency','동일 quick/admin request replay는 중복0. 다른 payload/action/event 의미는 conflict. 실제 동시 quick 요청도1저장+replay.'],
 ['H','confirmation','누락·false·문자열 true 거부 및 쓰기0. 명시 true만4테이블 원자 저장. 종·월·지역 기반 번식 추정 없음.'],
 ['I','admin 8 actions','실제 Cloudflare Access JWT로 approve/reject/unpublish/link/unlink/consent/visibility/site 모두 기존 의미·projection·revision·replay 확인.'],
 ['J','multi-review','하나 request에3 review events, sequence1/2/3·event_index0/1/2. compound UNIQUE 두 종류와 replay 의미 확인. count0 exact·1 unknown 보존.'],
 ['K','canonical / shadow','approved/pending/site/status/pagination/total/순서/응답 shape·헤더·공개 규칙 동등. shadow mismatch0. 사용자 read는 legacy 유지.'],
 ['L','maintenance','실제 public POST 및 Access admin mutation503 + Retry-After60, public/admin GET200. 시험 후 두 staging Worker maintenance 유지.'],
 ['M','mixed-version / peer','최종 v3 행렬14/14: 양방향 release·기대값·build·activation·schema·binding 불일치 차단. NORMAL 구 writer는 external freeze 필수임을 실제 확인.'],
 ['N','concurrency','실제 HTTP 동일 quick2개→1저장; 다른 quick→2저장; 동일 admin revision→200/409, 한 audit/revision만 증가. 직접 원격 CAS 경쟁도1commit.'],
 ['O','Turnstile','공식 테스트 설정의 실제 Siteverify: fail/spent403 쓰기0; pass/spent 동시201/201, 저장1, retry201. 브라우저 실토큰·특정 catch분기 실행을 입증한 것은 아님.'],
 ['P','sites','190 IDs 및 registry checksum/좌표 그대로 seed. 단일 D1.batch190 성공, replay0, invalid FK거부. 이름 병합·ID재발급 없음.'],
 ['Q','synthetic backfill','사용자 별도 승인 후21source→raw/checklist/sighting 각21, batch64. approved17/rejected4/siteNULL5/count1=13/countNULL8; count_accuracy unknown. 동일 manifest0/changed·partial 거부, 전체값 검증.'],
 ['R','remote limits',`fixture max38parameters/SQL570bytes/text516bytes. 성공 batch64/190. 미래3N+1+검증쿼리 예산 및 chunk 검증 필요; 계정 플랜·대규모 부하는 미확정.`],
 ['S','append-only','일반 application UPDATE/DELETE(raw/reviews/audit) 모두 거부. 원격 UPDATE trigger 고유 오류 확인. 관리자 직접 DB권한과 구분.'],
 ['T','synthetic purge','13/13. native만 실제 제거. 원본PK잔존0, 전체 application marker0, 타행 불변, 비민감 tombstone1. 원 quick재요청410, legacy는 decision required.'],
 ['U','Time Travel / restore','purge 이후 bookmark → synthetic assertion1 → 실제 staging restore. 모든 application10테이블·schema·migration ledger 동등, FK0/assertions0, purge tombstone 및410 유지. 계정 최대 보존일수 미확정.'],
 ['V','Worker rollback','두 staging Worker의 신규 maintenance 버전을 실제 배포 후 이전 검증 maintenance 버전으로 각각 rollback. 실제 활성version/binding/Access GET200/쓰기503 확인, DB불변.'],
 ['W','cutover rehearsal','14/14 순서 재연. 기존 schema migration/seed no-op, legacy21 cohort동등성, freeze/peer/shadow/내부smoke/실제 HTTP재개 검증. 최종maintenance. production drain시험 아님.'],
 ['X','코드 수정 / 재시험','원격 결함2개(역방향 release 미확인, binding없는 peer의 ready)를 먼저 재현. control.js/purge.js 최소수정, BUILD v3. 최종 local32·legacy106·2A17·protocol5·추가gate13 전부PASS.'],
 ['Y','남은 위험','실제 production freeze/drain, 실토큰 특정분기, 계정 최대retention, purge이전복구, legacy purge결정, 미래chunk/backfill은 별도 검토 필요. 아래 상세 위험 참조.'],
 ['Z','production go / no-go','NO-GO. Phase2C 검증 산출물 완료이며 운영 적용 승인이 아니다. 별도 검토·명시 지시 전 Phase2D/production 작업을 시작하지 않는다.']
];
writeFileSync(join(dir,'README.md'),`# Phase 2C — 원격 staging 검증 결과\n\nSTATUS: PHASE 2C COMPLETE (검증 범위와 제한 명시)\n\n${result.remote_checks}개 원격 검증/단계가 통과했다. 실제 결함2개를 수정했고 필수 로컬 재시험도 모두 통과했다. **Production 적용은 NO-GO**, Phase2D는 시작하지 않았다. 두 staging Worker는 maintenance 상태다.\n\n| 구분 | 항목 | 결과 |\n|---|---|---|\n${rows.map(r=>'| '+r.join(' | ')+' |').join('\n')}\n\n세부 실행 근거는 [execution-evidence.md](execution-evidence.md), 기계 판독 결과는 [test-results.json](test-results.json), 재개/종료 상태는 [CHECKPOINT.md](CHECKPOINT.md)에 있다.\n\n${risks.map(r=>'- '+r).join('\n')}\n\nProduction D1 writes = 0; Production migrations = 0; Production Worker deploys = 0; Production route changes = 0; Production secret changes = 0; commit = 0; push = 0.\n`);
let patch='';for(const name of ['control.js','purge.js']){let part;try{part=execFileSync('git',['diff','--no-index','--',join(LOCAL,'old-v1/reports-api/src/canonical',name),join(ROOT,'reports-api/src/canonical',name)],{encoding:'utf8',stdio:['ignore','pipe','pipe']});}catch(e){if(e.status!==1)throw e;part=e.stdout;}const path='reports-api/src/canonical/'+name;patch+=part.replace(/^diff --git .*$/m,`diff --git a/${path} b/${path}`).replace(/^--- .*$/m,`--- a/${path}`).replace(/^\+\+\+ .*$/m,`+++ b/${path}`);}
writeFileSync(join(dir,'phase2c-source-changes.patch'),patch);
const git=args=>execFileSync('git',args,{cwd:ROOT,encoding:'utf8',env:{...process.env,GIT_OPTIONAL_LOCKS:'0'}}).trim();
const additions=git(['ls-files','--others','--exclude-standard','docs/long-term-db-phase2c']).split('\n').filter(Boolean);
const recovered=suites.find(s=>s.name==='recovery').tests.find(t=>t.name.includes('rollback')).evidence;
const run=read('worker-deployments.json');
const evidence=`# Phase 2C execution evidence\n\nGenerated ${result.generated_at}. STATUS: PHASE 2C COMPLETE WITH DOCUMENTED LIMITATIONS. Production NO-GO.\n\n## 검증 요약\n\n| 원격 묶음 | 통과 | 원본 근거(ignored local) |\n|---|---:|---|\n| schema / PRAGMA | 1 | schema-verification.json |\n${suites.map(s=>'| '+s.name+' | '+s.passed+' | '+s.file+' |').join('\n')}\n\n합계 ${result.remote_checks}개 검사/단계. 실행 전후와 세부 assertion은 공개 가능한 test-results.json에 포함한다. 실패 재현과 초기 시험 harness의 null/빈값 기대 오류는 삭제하지 않고 .local/release-asymmetry-converged.json, rollout-before-peer-fix.json, rollout-remote-tests.json에 보존했다. 최종 v3 행렬은 별도14PASS. 최초 release 시도는 propagation 미수렴으로 inconclusive였고, 수렴 후 재현과 구분했다.\n\n최종 로컬: local32/32, legacy106/106(실제 assertion105 + helpers파일1), Phase2A17/17, protocol5/5, focused gate13/13. 근거 local32-final.log, legacy106-final.log, phase2a-final.log, protocol-final.log, gate-local-regression.json. 날씨·조석 코드/자료는 수정하지 않았으며 기존 Phase2B에서 사용자 승인한 조석 fixture882cm와 시험상한850cm 불일치 예외는 그대로다. 해당 비관련 시험을 다시 성공했다고 주장하지 않는다.\n\n## Git 및 파일 보존\n\n- branch ${preservation.branch}; 시작/종료 HEAD ${preservation.head}; local origin/main ${preservation.local_origin_main}. commit/push0.\n- 최신 remote main ${remote.remote_main}, 읽기전용 조회 ${remote.checked_at}; ahead${remote.ahead}/behind${remote.behind}. ${remote.files.join(', ')}만 원격차이. reports/Worker 코드 drift0, fetch/pull/merge0.\n- 시작511파일 재해시: 허용된 source변경2, 나머지509 동일, 누락0/예상밖변경0. Phase1/2A 문서 및 Phase2B 문서·기존 patch 보존.\n- 기존 추적5파일 diff는 Phase2B 그대로109추가/218삭제. Phase2C 앱수정2파일은 원래 미추적 canonical 디렉터리 안이라 git diff --stat에 잡히지 않는다. 별도 [phase2c-source-changes.patch](phase2c-source-changes.patch) 제공.\n\n## 실제 결함과 최소 수정\n\n1. v1에서 admin release만 변경했을 때 public503/admin200이 실제로 재현됐다. 역방향 peerRelease 일치 검사와 capability 공개를 추가했다.\n2. 첫 v2 원격 행렬에서 public peer binding 제거 후 public503/admin200이 재현됐다. 준비상태에 실제 peer.fetch와 gate token 존재를 요구했다.\n3. 최종 BUILD phase2c-dual-v3로 구 빌드와 mixed 상태도 차단. purge 역시 역방향 release 검사를 한다. SQL/schema hash/데이터 모델은 변경하지 않았다.\n\n수정 파일: reports-api/src/canonical/control.js, reports-api/src/canonical/purge.js. .local/old-v1은 수정 전 소스의 검토·mixed-build용 사본이다. 새 gate는 배포 propagation이 끝나기 전에 준비됐다고 가정하지 않는다. 각 사례에서 HTTP 및 peer service binding 양쪽이 목표 capability를 실제 관찰할 때까지 확인했다.\n\n## 리소스 및 인증 경계\n\n- 계정 ${ACCOUNT}; staging D1 birdmap-reports-staging / ${STAGING_DB}, 생성2026-09-26T10:27:04.772Z. production ${PRODUCTION_DB}와 다름.\n- 두 staging Worker의 실제 REPORTS_DB와 REPORTS_PEER 확인. routes/custom domains 없음. internet 시험token과 internal gatetoken은 각각 별도이며 값은 보고서에 없다. preview URLs 비활성.\n- staging Access app6e5dbd14-6a92-4a8a-8feb-6239d929983c 및 policydc7eaf34-a5c2-4097-8933-8324168c146d만 생성. 기존 team 사용, staging admin /admin 경로, 소유자만 허용. production Access 수정0. Access관리API 권한403이므로 전체 Access설정 불변을 API snapshot으로 증명했다고 주장하지 않는다.\n- 공식 cloudflared CLI로 실제 Access JWT 취득, issuer/audience확인. actual Access HTTP와 synthetic actor persistence 시험은 구분한다. JWT·OAuth·gate·salt는 .local/의 ignored 파일 외 보고서에 기록하지 않았다. OAuth401은 공식 Wrangler whoami refresh 후 정상 재개됐다.\n- Wrangler4.137 / Node24.18, 실제 remote D1.batch/Service Binding 사용. 로컬 Miniflare는 별도 로컬 회귀검증이다.\n- staging deploy command 기록${run.length}건; 별도 실제 rollback2회. 최종 active public ${recovered.deployed.public.version}; admin ${recovered.deployed.admin.version}. 두 Worker maintenance, GET정상/POST503.\n\n## 스키마·원자료·count\n\n0000_legacy.sql 및0001_core.sql은 저장소/Phase2A SQL bytes 그대로 공식 Wrangler 적용. ${schema.remote_hash}가 저장소/local/remote에서 같고 schemaReady true. index/trigger/CHECK/FK/UNIQUE 및 PRAGMA 차이0.\n\n합성21건에 대해서만 별도 승인 후 batch64를 실행했다. manifest ${prep.manifest_sha256}; transform ${prep.transform_version}; registry ${prep.registry_revision}. approved17/rejected4/siteNULL5/count1 13/countNULL8. 기존1을 unknown이라는 이유로 NULL로 지우지 않았다. 여러 종+공유count는 원값 호환영역에 보존하고 sighting별 countNULL; 종별 분할/중복배분 없음. 기존21행을 최초 제출 원문으로 정의하거나 review/audit 이력을 만들지 않았다. native 시험 추가 후에는 cohort21 전체값을 따로 재검증했고, 기존 whole-snapshot backfill verifier를 변경해 native행을 무시하도록 완화하지 않았다.\n\n## 복구와 purge 범위\n\nPurge13PASS: 실제 native10040만 제거. guard/revision/peer/권한/확인, batch8/14실패 rollback, 원래PK잔존0·모든 application text의 고유 marker0·타행불변·정확히9필드 tombstone1. 정확좌표·메모·payload·species/location fingerprint가 tombstone에 없고 request_fingerprintNULL/event_count0이다. 재요청410. legacy삭제는 명시적 결정 필요 상태 유지.\n\nTime Travel은 purge 이후 모든 writer 시험을 종료하고 maintenance에서 새 bookmark를 얻었다. 고정 stagingDB에 assertion probe1을 쓴 후 실제 restore했고, 모든10 application tables/data와 sqlite_schema 및 d1_migrations ledger가 기준과 같았다. _cf_KV 등 Cloudflare 내부 metadata byte까지 동일하다는 주장은 하지 않는다. 생성1분 후 요청 timestamp의 bookmark도 조회됐다. 최대 보존은 공식 Free7일/Paid30일이며 이 새 DB의 실제 최대기간/계정플랜은 미확정이다. [Time Travel](https://developers.cloudflare.com/d1/reference/time-travel/).\n\n두 Worker rollback은 v3·Access·staging bindings가 확인된 maintenance 버전만 대상으로 했다. CLI성공 외 activeversion100%, 서비스binding, authenticatedGET200/POST503 및 DBdigest불변까지 확인했다. [Worker rollback](https://developers.cloudflare.com/workers/versions-and-deployments/rollbacks/).\n\n## 한도와 다음 전환 조건\n\n실측: sites190 statements / backfill64 statements 성공. 최대 bound38/SQL570bytes/text516bytes/serializedrow1033bytes. 공식 개별statement100KB, bound100, row2MB, batch30초, invocation쿼리Free50/Paid1000, 동시연결6. [D1 limits](https://developers.cloudflare.com/d1/platform/limits/).\n\n향후 3N+1삽입 + 검증16쿼리 예산을 가정하면 floor((Q-17)/3)=Free11/Paid327행이라는 계획상 상한이다. 보수적인 다음시험10/100행도 아직 실행한 대규모 보증이 아니다. 지금의 부분상태 거부 및 최종 run ledger 원칙을 깨는 단순chunk 적용은 금지한다.\n\n공식 Turnstile 테스트 pass/fail/spent만 사용했다. production secret 복사0. 동시pass/spent201/201은 원격 특정catch분기 실행이나 실제브라우저token의 소모를 증명하지 않는다. [Turnstile testing](https://developers.cloudflare.com/turnstile/troubleshooting/testing/).\n\n| 전환 단계 | GO 조건 | NO-GO 조건 |\n|---|---|---|\n| 외부freeze·drain | 양쪽 모든 writer503, 진행요청 종료 확인 | NORMAL 구writer 접근 가능/진행중write |\n| schema·seed | 실제 schemaReady/ID190/checksum 일치 | DDL/FK/registry drift |\n| snapshot·backfill | immutable manifest와 전체값 보존, 승인된실행 | changedmanifest/partialstate/원본수변화 |\n| 새Worker 배포 | 양쪽 실제v3 capability/release/activation/schema일치 | mixed/unreachable/productionbinding |\n| shadow·smoke | shape/count/좌표공개규칙 동일, 원자성/CAS검증 | mismatch/중복/부분쓰기 |\n| reopen | 실제HTTP/Access성공,rollback·restore담당/기준승인 | freeze해제방법·복구정책 미확정 |\n\n기설치 staging에서14단계를 순서대로 재연했지만 실제 production drain과 첫migration 전체절차를 한 번에 재현한 것은 아니다. 아래 위험을 해소하고 별도 승인 후 다음Phase를 판단해야 한다.\n\n${risks.map(r=>'- '+r).join('\n')}\n\n## 최종 production 읽기전용 검증\n\n${end.checked_at}: ${JSON.stringify(result.production.counts)}. SELECT metadata rows_written0/changed_dbfalse 확인. production Worker 배포·binding snapshot 시작과 동일. public57849940-c4b6-494f-88e9-27ec2e327cd9; admin1eebcf4e-e7e0-4b94-bdf2-65c44962c21a. 행수만으로 외부행위 전체의 무변경을 증명하지는 않으며, 아래0은 이 작업의 실제 실행 내역이다.\n\n${Object.entries(counters).map(([k,v])=>'- '+k+' = '+v).join('\n')}\n\n## 파일 전체 목록\n\n기존 파일 수정2:\n\n- reports-api/src/canonical/control.js\n- reports-api/src/canonical/purge.js\n\nPhase2C 생성/갱신 파일(.local 제외):\n\n${additions.map(p=>'- '+p).join('\n')}\n\n.local은 ignored 시험설정/합성fixture/토큰/로그/증거용이며 공유용 산출물에서 제외한다. 기존 Phase2B patch/untracked 목록은 당시 snapshot이므로 Phase2C추가분으로 덮어쓰지 않았다.\n`;
writeFileSync(join(dir,'execution-evidence.md'),evidence);
writeFileSync(join(dir,'CHECKPOINT.md'),`# Phase 2C CHECKPOINT\n\nSTATUS: PHASE 2C COMPLETE\n\n- Completed ${result.generated_at}. Verification complete with documented limits; Production NO-GO. Phase2D NOT STARTED.\n- Branch main, HEAD ${preservation.head}, local origin/main ${preservation.local_origin_main}. Latest readonlyremote ${remote.remote_main} (${remote.checked_at}), onlyfive weather/tide JSON differences. No fetch/pull/commit/push.\n- Staging D1 birdmap-reports-staging / ${STAGING_DB}; protectedproduction ${PRODUCTION_DB}. Workers ${WORKERS.join(', ')}. Staging-only Access.\n- Remote95 checks/steps PASS, local32/legacy106/2A17/protocol5/gate13 PASS. Exact per-suite tests and limitations in test-results.json and execution-evidence.md.\n- User approved synthetic21backfill, applied/verified once. Nativepurge completed once. TimeTravelrestore completed once usingpurge-afterbookmark. Two actualWorkerrollbacks completed. DO NOT repeat destructive staging tests or recreate resource on resume.\n- Final publicversion ${recovered.deployed.public.version}; adminversion ${recovered.deployed.admin.version}. Both READ_ONLY_MAINTENANCE, actual authenticatedGET200/POST503, stagingDB/peer bindings confirmed. Final counts ${JSON.stringify(final.state.counts)}; FK0/assertions0.\n- Appsource changes exactly2: control.js reciprocalpeerRelease/readinessbinding+gatetoken/BUILDv3; purge.js reciprocalreleasecheck. Realremote defects reproduced before fixes. Data model/SQL unchanged. Existing511files:509unchanged +2authorized; unexpected0.\n- Completedfiles: see execution-evidence.md fullinventory. All newPhase2C scripts/evidence underdocs/long-term-db-phase2c; .local ignoredsecrets must never be printed/shared. Original Phase1/2A/2B artifacts retained.\n- Last successful steps: finalstaging andproduction readonly snapshots, remotemain drift, preservation, finalevidence generation.\n- Remaining implementation work inPhase2C:none. Remaining acceptance risks: actualproduction externalfreeze/drain; browserrealTurnstile branch proof; accountmaximumretention; prepurge restore policy; legacypurge decision; futurelargechunk/backfill validation. No automaticPhase2D.\n- Next action: waitforuser review/instruction. Ifaskedtoinspect, readthischeckpoint andcurrentrealstates; do not reruncompletedrestore/backfill/purge.\n- Production D1 writes0/migrations0/Worker deploys0/routes0/secrets0/bindings0/Accesschanges0/export0/restore0; commit0/push0.\n`);
console.log(JSON.stringify({status:result.status,remoteChecks:result.remote_checks,local:[32,106,17,5,13],productionDecision:result.production_decision,files:additions.length+2}));
