# 전국 탐조지도 AI 공동 작업 규칙

## 1. 프로젝트 기본 원칙
- 현재 실제 서비스 기준은 main 브랜치의 index.html이다.
- 정상 작동하는 기존 기능을 임의로 삭제, 단순화, 재설계하지 않는다.
- 사용자가 요청하지 않은 리팩터링은 하지 않는다.
- 불확실한 내용은 추측하지 않는다.
- 수정 범위를 최소화한다.

## 2. AI 역할
- ChatGPT: 전체 설계, 작업 범위 결정, 데이터 및 결과 검증
- Codex: 주 개발 및 기능 구현
- Claude Code: 저장소 분석, 코드 리뷰, 오류 추적, 필요 시 수정
- Gemini: 향후 최종 교차 검토 및 누락 점검
- GitHub main 브랜치: 모든 AI가 공유하는 단일 기준 상태

## 3. 작업 순서
- 한 번에 하나의 AI만 코드를 수정한다.
- 작업 시작 전 반드시 최신 main 상태를 확인한다.
- 이전 AI의 Commit과 Push가 끝난 뒤 다음 AI가 작업한다.
- 동시에 같은 파일을 수정하지 않는다.
- 작업 완료 후 변경 파일, 변경 내용, 테스트 결과를 보고한다.
- 실제 수정 작업에서는 사용자의 지시가 있으면 Commit과 Push까지 수행한다.

## 4. 데이터 기준
- index.html 내부 siteData는 현재 실제 서비스의 런타임 데이터다.
- 저장소 내 엑셀 파일은 오프라인 원본 DB다.
- README, 엑셀 파일명, 조석 스크립트가 서로 다른 버전을 참조할 수 있으므로 수정 전 실제 최신 기준 파일을 반드시 확인한다.
- 기준 엑셀을 임의로 결정하지 않는다.
- 사용자가 지정한 최신 업데이트용 엑셀을 우선한다.
- 데이터 구조를 변경할 때 기존 필드를 삭제하지 않는다.

## 5. 좌표 검증 원칙
탐조지 좌표를 수정하거나 추가할 때 반드시 다음을 대조한다.
- 기준 엑셀 위도·경도
- index.html siteData의 lat·lon
- 카카오맵 좌표
- Windy 좌표
- weather-proxy/src/sites.js 좌표
- 조석 매핑 좌표 또는 관측소 연결 정보가 관련된 경우 해당 정보

좌표는 임의 보정하지 않는다.
불일치가 있으면 수정 전에 보고한다.
작업 완료 보고에 좌표 일치 여부를 반드시 포함한다.

## 6. 지도 연결 원칙
- 네이버지도는 명칭 검색을 기본으로 한다.
- 카카오맵은 좌표 이동을 기본으로 한다.
- Windy는 해당 탐조지 좌표를 사용한다.
- eBird는 저장된 Hotspot ID가 있을 때 연결한다.
- KSA는 섬 탐조지에 필요한 경우 기존 구조를 유지한다.
- 기존 팝업 버튼 구조를 임의로 늘리지 않는다.
- 카카오맵 버튼은 기존 1개 구조를 유지한다.

## 7. 기상 기능 주의사항
- 실시간 기상은 Cloudflare Worker와 연결되어 있다.
- index.html의 siteData와 weather-proxy/src/sites.js는 현재 수동 이중 관리 가능성이 있으므로 좌표 또는 탐조지 변경 시 양쪽을 반드시 확인한다.
- weather-proxy/src/sites.js의 id는 index.html의 siteData id와 일치해야 한다.
- pelagic 등 기상 분기 관련 필드도 일치해야 한다.
- 실시간 기상, 오늘 탐조 적합도, 내일 오전·오후 예보를 혼동하지 않는다.
- 자동 생성 파일을 수동 수정하지 않는다.

## 8. 자동 생성 파일
다음 파일은 GitHub Actions 또는 스크립트가 덮어쓸 수 있으므로 직접 수정 전에 생성 구조를 확인한다.
- weather_today.json
- tide_today.json
- notices.json
- 기타 자동 생성 데이터 파일

자동 생성 파일을 직접 수정한 경우 다음 자동 실행에서 사라질 수 있음을 반드시 고려한다.

## 9. index.html 수정 원칙
- index.html은 현재 약 8,785줄 규모의 실제 서비스 핵심 파일이다.
- 대규모 리팩터링을 금지한다.
- 기존 팝업 함수와 버전 누적 구조는 사용자의 별도 승인 없이 통합하거나 삭제하지 않는다.
- siteData 전체를 수정할 때는 일반 텍스트 Diff만 믿지 말고 JSON 구조와 개체 수를 검증한다.
- 수정 전후 탐조지 개체 수와 id 중복 여부를 확인한다.
- 검색, 필터, 마커, 팝업, 모바일 화면에 미치는 영향을 점검한다.

## 10. 테스트 원칙
수정 내용에 따라 가능한 범위에서 다음을 확인한다.
- HTML 및 JavaScript 문법 오류
- 탐조지 개체 수
- id 중복
- 검색
- 필터
- 마커 표시
- 팝업
- 모바일 화면
- 네이버지도
- 카카오맵
- Windy
- eBird
- KSA
- 실시간 기상
- 오늘 탐조 적합도
- 내일 오전·오후 예보
- 조석 정보

모든 작업에서 전체 항목을 기계적으로 테스트할 필요는 없지만, 수정 영향 범위에 해당하는 기능은 반드시 검증한다.

## 11. 금지사항
- 사용자 승인 없는 대규모 리팩터링
- 정상 기능 삭제
- 임의 좌표 보정
- 기준 엑셀 임의 변경
- 사용자가 요청하지 않은 UI 버튼 추가
- 여러 AI의 동시 수정
- 자동 생성 파일의 구조를 확인하지 않은 직접 수정
- API 키나 비밀정보를 코드, 로그, 보고서에 노출
- 테스트하지 않은 상태에서 정상 완료라고 보고

## 12. 작업 완료 보고 형식
모든 수정 작업 완료 후 다음 형식으로 보고한다.

1. 작업 목적
2. 변경 파일
3. 변경 내용
4. 테스트 결과
5. 좌표 검증 결과(관련 작업일 경우)
6. 기존 기능 영향 여부
7. 발견된 미해결 문제
8. Commit ID와 Push 여부
